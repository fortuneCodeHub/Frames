stands for php server api name
/**
 * You can run php api in two ways which are;
 * ---CGI- this is normal one we run through the browser, another name for it is apache2handler
 * ---CLI- this one is run through the command line prompt
 */
#this is the function
// print_r(php_sapi_name()); 
// echo "<br>";
#this is getting the sapi name with a CONSTANT
// print_r(PHP_SAPI);
/** the $argv variable breaks string written contents in the cli into an array */